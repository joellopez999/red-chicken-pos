# Migration fixtures

Sample CSV for the products + categories cutover import (#321).

- **`sample_products.csv`** — happy-path catalog (no real customer PII).
- Column map and runbook: **`docs/0062-pos-migration-import.md`**.
